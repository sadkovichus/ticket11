{
    "build": {
      "env": {
        "DATABASE_URL": "./src/server/prisma/dev.db"
      },
      "commands": {
        "prebuild": "npm run migrate",
        "build": "next build"
      }
    }
  }
  